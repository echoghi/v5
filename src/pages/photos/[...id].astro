---
import { getCollection } from 'astro:content'

export async function getStaticPaths() {
  const photos = await getCollection('photos')
  return photos.map((collection) => ({
    params: { id: collection.id },
  }))
}

return Astro.redirect('/photos', 301)
---
